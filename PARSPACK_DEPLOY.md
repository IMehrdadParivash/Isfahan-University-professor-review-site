# استقرار کم‌هزینه روی پارس‌پک

این نسخه برای هاست لینوکس اشتراکی دارای **PHP 8.1+، PDO MySQL، mbstring و MySQL/MariaDB** طراحی شده است. Node.js، سرویس دائمی، Composer و build روی سرور لازم نیست.

## پلن انتخاب‌شده

برای شروع، **هاست اشتراکی/لینوکس استارتر ایران با DirectAdmin** کافی است: ۱ گیگابایت SSD، یک vCPU و ۲ گیگابایت RAM. پیش از پرداخت در تیکت فروش تأیید کنید که ساخت دیتابیس MySQL، انتخاب PHP 8.1 یا بالاتر، Cron و Terminal/SSH برای اجرای یک‌بارهٔ Installer در همان پلن فعال‌اند. اگر Terminal ندارد، از پشتیبانی بخواهید `php bin/install.php` را یک‌بار اجرا کند. پلن رایگان به‌دلیل فضای و ترافیک بسیار محدود برای انتشار عمومی این سامانه مناسب نیست.

## ۱. ساخت بسته

```bash
python3 tools/build-parspack-release.py
```

فایل `.release/parspack-site.zip` را در `public_html` بارگذاری و همان‌جا Extract کنید. فایل `.htaccess` باید همراه بسته منتقل شود.

## ۲. ساخت دیتابیس و تنظیم رازها

در پنل هاست یک دیتابیس MySQL و کاربر محدود به همان دیتابیس بسازید. سپس `config.example.php` را به `config.php` کپی کنید و مقادیر واقعی را قرار دهید.

رازها را محلی بسازید و در Git ثبت نکنید:

```bash
php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
php -r "echo password_hash('YOUR-STRONG-ADMIN-PASSWORD', PASSWORD_DEFAULT), PHP_EOL;"
```

مجوز `config.php` را در صورت پشتیبانی هاست روی `600` یا حداقل محدودترین حالت قابل‌اجرا تنظیم کنید.

## ۳. نصب ساختار و فهرست استادان

از Terminal/SSH پنل و داخل `public_html` اجرا کنید:

```bash
php bin/install.php
```

اسکریپت، Schema مناسب را نصب و فهرست ۷۴۳ استاد و درس‌های شناخته‌شده را از بستهٔ دادهٔ موجود وارد می‌کند. مسیر `bin` و فایل تنظیمات از وب مسدود شده‌اند.

## ۴. دامنه و Cloudflare

دامنهٔ `.ir` را به DNS موردنظر متصل کنید. اگر Cloudflare استفاده می‌شود، SSL را روی **Full (strict)** بگذارید، HTTPS را اجباری کنید و ابتدا `trust_cloudflare_ip_header` را `false` نگه دارید. فقط وقتی دسترسی مستقیم به Origin بسته شده و درخواست‌ها الزاماً از Cloudflare می‌آیند، آن را `true` کنید.

در `config.php` مقدار `base_url` و در `robots.txt` آدرس Sitemap را با دامنهٔ واقعی جایگزین کنید. سلامت API را با `/api/index.php?route=health` بررسی کنید.

## ۵. Backup و نگهداری

- Backup روزانهٔ دیتابیس را از پنل هاست فعال کنید و حداقل ۷ نسخهٔ روزانه و ۴ نسخهٔ هفتگی نگه دارید.
- یک نسخه خارج از همان هاست نگه دارید؛ Backup روی همان سرور در خرابی کامل کافی نیست.
- با Cron روزانه اجرا کنید: `php /FULL/PATH/public_html/bin/maintenance.php`.
- دوره‌ای بازیابی Backup را روی یک دیتابیس آزمایشی امتحان کنید.

## ۶. تنظیمات امنیتی پنل

- پوشهٔ `admin` را در صورت امکان علاوه بر رمز برنامه با IP Allowlist یا Basic Auth پنل محافظت کنید.
- نمایش خطاهای PHP خاموش و ثبت خطا در فایل غیرعمومی روشن باشد.
- PHP را به‌روز نگه دارید و دسترسی نوشتن عمومی به فایل‌های برنامه ندهید.
- حساب‌ها پیش‌فرض خاموش‌اند. فقط پس از آماده‌کردن سیاست بازیابی حساب، `accounts_enabled` را فعال کنید.

## Rollback

فایل‌های نسخهٔ قبلی و Dump دیتابیس قبل از انتشار را نگه دارید. برای Rollback ابتدا کد قبلی را برگردانید؛ مهاجرت‌های دیتابیس را بدون Backup دستی معکوس نکنید.
