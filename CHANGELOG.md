# تاریخچه تغییرات

این فایل تغییرات مهم و قابل‌انتشار پروژه را به‌صورت خلاصه ثبت می‌کند.

## 2026-08-30 — سامانهٔ کامل ثبت تجربه

- افزوده‌شدن API بدون وابستگی با PHP/PDO و Schemaهای MySQL/SQLite.
- ثبت نظر ناشناس، نام مستعار، نام واقعی و حساب اختیاری با ۹ معیار امتیاز.
- رأی مفید/غیرمفید، Report، Rate Limit، Honeypot، CSRF، HMAC و تشخیص تکرار.
- پنل مدیریت با صف گزارش، دلیل اجباری و Audit Log بدون حذف مستقیم نظر.
- صفحهٔ مستقل استاد، آمار Cacheشده، Structured Data و Sitemap پویا.
- سیاست حریم خصوصی، قواعد انتشار، بستهٔ پارس‌پک و Cron فراموشی شناسه‌های فنی.
- فیلتر درس در جست‌وجو و فیلترهای دقیق‌تر بر اساس آمار نظرهای جدید.

پیاده‌سازی و بازبینی این مرحله: **Codex (OpenAI)**.

## 2026-08-29 — پاک‌سازی آواتار و پولیش مخزن

انجام‌دهنده: **GPT-5.6 Sol (OpenAI)**

### رابط کاربری

- حذف کامل تصویر آواتار از صفحهٔ بارگذاری.
- حذف preload مربوط به تصویر آواتار از `index.html`.
- تبدیل انیمیشن بارگذاری به نسخهٔ سبک‌تر مبتنی بر ترمینال، متن وضعیت و نوار پیشرفت.
- حفظ رفتار fail-safe و پشتیبانی از `prefers-reduced-motion`.

### دارایی‌ها و انتشار

- حذف فایل‌های WebP آواتار و poseهای قدیمی از مخزن.
- حذف وابستگی build/release به پوشهٔ `assets/avatar`.
- جلوگیری از ورود دوبارهٔ فایل‌های آواتار به بستهٔ انتشار از طریق تست ایمنی release.
- به‌روزرسانی شناسهٔ Git blob فایل runtime لودر در `dataset-manifest.json`.

### مستندات

- بازنویسی و مرتب‌سازی README فارسی.
- به‌روزرسانی ساختار مخزن در مستندات.
- اضافه‌شدن توضیح روشن دربارهٔ هدف پروژه، داده، حریم خصوصی، آزمون و انتشار.
- ثبت نام انجام‌دهندهٔ این مرحله از بازبینی و پولیش.
